<% if @answer.errors.any? %>
  $('.errors').html('<%= j render 'shared/errors', resource: @answer %>')
<% else %>
  $('.answers').html('<%= j render @answer %>')
  $('#answer_body').val('')
<% end %>
